# Streamlit Cloud installs this file when deploying frontend/app.py.
# Keep this list frontend-only to avoid backend ML build failures on hosted envs.
streamlit>=1.40
requests==2.32.3
python-dotenv==1.0.1
